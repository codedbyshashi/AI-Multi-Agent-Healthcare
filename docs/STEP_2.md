# Step 2 - API Integration

Connected the React dashboard to the FastAPI backend.

Included:
- PDF upload
- `POST /analyze/` request using `FormData`
- Loading state
- Timeout, network, and server error handling
- Result rendering for:
  - `selected_tool`
  - `workflow_plan`
  - `analysis`
